{% block body %}
    {% set flash_types = ['success', 'danger'] %}

    {% for flash_type in flash_types %}
        {% for message in app.flashes([id, '-', flash_type]|join) %}
            <div class="alert alert-{{ flash_type }}">
                <p class="color-primary-lighter">{{ message }}</p>
            </div>
        {% endfor %}
    {% endfor %}
{% endblock %}
